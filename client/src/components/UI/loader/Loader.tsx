import React, {FC} from 'react';
import './Loader.css';

const Loader: FC = () => {
    return (
        <div className='flex-container'>
            <div className='loader'/>
        </div>
    );
};

export default Loader;